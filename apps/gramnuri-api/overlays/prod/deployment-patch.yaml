apiVersion: apps/v1
kind: Deployment
metadata:
  name: gramnuri-api
spec:
  selector:
    matchLabels:
      app: gramnuri-api
  template:
    spec:
      replicas: 2
      imagePullSecrets:
        - name: ghcr-image-pull-secrets
      containers:
        - name: gramnuri-api
          envFrom:
            - secretRef:
                name: gramnuri-secrets
