apiVersion: v1
kind: Secret
metadata:
  name: openclaw-env-secret
  namespace: openclaw
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "replace-with-actual-key"
  OPENCLAW_GATEWAY_TOKEN: "replace-with-actual-token"
